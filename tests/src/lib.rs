//! End-to-end integration tests for the Dangerzone port.
//!
//! The actual tests live in the `tests/` subdirectory and drive the
//! `dz-dangerzone` binary as a subprocess. This library target exists so the
//! package has something to build; it exports no items.
